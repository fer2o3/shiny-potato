#include "aoc.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void solve_23(char **lines, int line_count) {
    (void)lines;      // Kachow !
    (void)line_count; // This shit is just to suppress warning for now

    int part1 = 19046, part2 = 47484;

    // KACHOW: This was actually done by hand
    //         I'm pretty sure this would have taken be hours if not
    //         I'll "probably" revisit this later

    printf("Part 1: %d\nPart 2: %d\n", part1, part2);
}
